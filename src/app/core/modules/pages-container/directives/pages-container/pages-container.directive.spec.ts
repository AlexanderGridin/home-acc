import { PagesContainerDirective } from './pages-container.directive';

describe('PagesContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new PagesContainerDirective();
    expect(directive).toBeTruthy();
  });
});
